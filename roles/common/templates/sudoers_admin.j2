# {{ ansible_managed }}
# Файл управляется Ansible — правки руками будут перезаписаны при следующем прогоне.
# Даёт админу право выполнять sudo без ввода пароля (удобно для автоматизации).
# ВАЖНО: права на этот файл должны быть 0440 — их ставит задача в tasks/main.yml.

{{ admin_user }} ALL=(ALL) NOPASSWD:ALL